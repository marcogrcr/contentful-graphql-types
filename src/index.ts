/**
 * Returns the sum of two operands.
 * @param first The first operand.
 * @param second The second operand.
 * @returns The sum of the first and second operands.
 */
export function sum(first: number, second: number): number {
  return first + second;
}
